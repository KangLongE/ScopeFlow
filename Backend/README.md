# ScopeFlow Backend

백엔드 분리 구현을 위한 Node.js + TypeScript 작업 공간입니다.

현재는 프레임워크와 API 코드를 확정하지 않고 TypeScript 실행 환경만 준비했습니다. DB·인증·API 설계가 정해지면 이 폴더에서 구현합니다.

```powershell
npm install
npm run typecheck
```

비밀값은 커밋하지 말고 로컬 `.env.local` 또는 배포 플랫폼의 환경변수로만 관리하세요.
