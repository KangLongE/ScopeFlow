import Link from "next/link";
import { currentContext, listProjects } from "@/lib/queries";
import { Badge, Card, Empty, PageHeading } from "@/components/ui";
import { won } from "@/lib/model";
export default async function Requests() { const ctx=await currentContext(); const rows=(await listProjects(ctx.workspaceId)).flatMap(project=>project.changeRequests.map(change=>({change,projectName:project.name}))).sort((a,b)=>b.change.createdAt.localeCompare(a.change.createdAt)); return <><PageHeading eyebrow="CHANGE REQUESTS" title="변경 요청" description="새로운 요청의 범위와 영향을 확인하고, 고객과 변경 내용을 합의하세요." /><Card title={`모든 변경 요청 ${rows.length}`}>{rows.length ? <div className="table-wrap"><table><thead><tr><th>프로젝트 / 변경 요청</th><th>범위 판정</th><th>추가 금액</th><th>상태</th></tr></thead><tbody>{rows.map(({change:c,projectName})=><tr key={c.id}><td><Link href={`/projects/${c.projectId}/changes#${c.id}`}><strong>{projectName} · CR #{c.number}</strong><small>{c.request.slice(0,80)}</small></Link></td><td><Badge value={c.review?.classification || c.analysis?.classification || "UNCERTAIN"} /></td><td>{won(c.amount)}</td><td><Badge value={c.status} /></td></tr>)}</tbody></table></div> : <Empty title="합의한 범위를 지켜보세요" description="진행 중인 프로젝트에 새로운 요청이 들어오면 변경 요청을 만들어 관리하세요." href="/projects" cta="프로젝트 확인" />}</Card></>; }
