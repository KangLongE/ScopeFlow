import Link from "next/link";
import { currentContext } from "@/lib/queries";
import { aiConfigured, configuredProvider, timeoutMs } from "@/lib/ai/provider";
import { modelFor } from "@/lib/ai/prompts";
import { Card, Field, PageHeading } from "@/components/ui";
import { MutationForm } from "@/components/form";
import { labels } from "@/lib/model";
export default async function AISettings() { const ctx=await currentContext(); return <><PageHeading eyebrow="AI & CREDITS" title="AI 및 Credit" description="AI 사용량을 예측하고, 팀의 사용 한도를 관리하세요." /><div className="tabs"><Link href="/settings">Workspace</Link><Link href="/settings/rates">작업 단가</Link><Link className="active" href="/settings/ai">AI 및 Credit</Link></div><div className="two-col"><Card title="월별 Credit 한도" hint="매월 1일 UTC 기준으로 새 사용 기간이 시작됩니다."><div className="card-body"><MutationForm action="credits.save" disabled={ctx.role!=="OWNER"} submit="Credit 설정 저장"><div className="form-grid"><Field label="Workspace 월 한도" name="creditLimit" type="number" min={0} max={10000} value={ctx.workspace.creditLimit} required /><Field label="사용자 월 한도 (전체 Workspace 합산)" name="userCreditLimit" type="number" min={0} max={10000} value={ctx.workspace.userCreditLimit} required /></div><h3>작업별 Credit</h3><div className="form-grid">{["initial","questions","requirements","estimate","summary","compare"].map(action=><Field key={action} label={labels[action]} name={`creditCosts.${action}`} type="number" min={1} max={100} value={ctx.workspace.creditCosts[action]} required />)}</div><p className="text-xs">한도를 초과하면 AI 요청이 차단됩니다. 동일 입력의 저장된 결과를 재사용할 때는 추가 Credit이 차감되지 않습니다.</p></MutationForm></div></Card><div className="stack"><Card title="AI 연결 상태"><div className="card-body"><div className="notice"><span>{aiConfigured() ? "● AI API 연결 설정 완료" : "○ AI API 키 설정 필요"}</span></div><p className="prose">{aiConfigured() ? "실제 API 요청 결과는 사용량 페이지에서 확인할 수 있습니다." : `서버의 .env.local에 ${configuredProvider() === "groq" ? "GROQ_API_KEY" : "AI_API_KEY"}를 설정하고 재시작하면 AI 기능이 활성화됩니다. 설정 전에는 요구사항과 견적을 직접 입력할 수 있습니다.`}</p><dl className="info-grid mt-6"><div><dt>Provider</dt><dd className="mono">{configuredProvider()}</dd></div><div><dt>요약 모델</dt><dd className="mono">{modelFor("summary")}</dd></div><div><dt>분석 모델</dt><dd className="mono">{modelFor("initial")}</dd></div><div><dt>심층 비교 모델</dt><dd className="mono">{modelFor("compare",true)}</dd></div><div><dt>요청 제한 시간</dt><dd>{timeoutMs()/1000}초</dd></div></dl><p className="text-xs mt-5">모델별 비용 계산에는 서버의 AI_MODEL_PRICES 설정이 필요합니다. 미설정 모델의 추정 비용은 0으로 기록되므로 공급자의 실제 청구액과 다를 수 있습니다.</p></div></Card><Card title="비용 보호"><div className="card-body"><p className="prose">사용자별 분당 8회 AI 요청<br />오류 재시도 최대 1회<br />동일 요청의 중복 실행 차단<br />출력 토큰 상한 및 요청 Timeout<br />사용자·Workspace Credit 동시 검사</p><Link className="inline-link inline-block mt-5" href="/usage">이번 달 사용량 확인 →</Link></div></Card></div></div></>; }
