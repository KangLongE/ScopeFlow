import { Field, Textarea } from "./ui";
import { labels, type RequirementInput } from "@/lib/model";
export function RequirementFields({ value }: { value?: RequirementInput }) { return <><div className="form-grid"><Field label="기능 / 요구사항 이름" name="title" value={value?.title} required /><Field label="분류" name="category" value={value?.category} placeholder="예: 인증, 결제, 디자인" required /><label>유형<select name="type" defaultValue={value?.type || "FEATURE"}>{["FEATURE","NON_FUNCTIONAL","DESIGN","INFRA","EXTERNAL_INTEGRATION"].map(v=><option key={v} value={v}>{labels[v]}</option>)}</select></label><label>우선순위<select name="priority" defaultValue={value?.priority || "MEDIUM"}>{["HIGH","MEDIUM","LOW"].map(v=><option key={v} value={v}>{labels[v]}</option>)}</select></label></div><Textarea label="상세 설명과 완료 기준" name="description" value={value?.description} required placeholder="어떤 동작이 필요하고, 어디까지 제공하는지 명확하게 적어주세요." /></>; }
